--- @meta

--- @class stoslib: oslib
os = {}

--- Returns the name of the operating system this is currently running on.
--- @return "windows" | "linux" | "android" | "macos" | "ios" | "web" | "bsd"
function os.name() end
